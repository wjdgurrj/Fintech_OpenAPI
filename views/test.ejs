<html>
  <h1>EJS 사용 확인</h1>
  <h2>페이지가 잘 나옵니다</h2>
  <input type="text" id = "inputText"></input>
  <button type="button" id = "sendButton">보내기</button>
  <script src="https://code.jquery.com/jquery-3.5.1.js" integrity="sha256-QWo7LDvxbWT2tbbQ97B53yJnYU3WhH/C8ycbRAkjPDc=" crossorigin="anonymous"></script>
  <script>
      $("#inputText").val();// # == id, jquery 사용은 $,#은 id를 의미,#id를 가져온다는 것
      $("#sendButton").click(function(){  
          var inputText = $("#inputText").val(); // eventbinding 하면 된다. 
          $.ajax({// ajax 기존 페이지를 변경하면서 비동기적으로 데이터를 받아오기위한 방식 , 데이터를 보내는 방법, 단순히 받아오기 위한 방법 등이 있다.
            url:'/getDataTest',
            type : 'POST',
            data : {
                userText : inputText,
                sendTime:new Date()
            },
            success:function(data){ // 요청이 성공을 했을 때
                alert(data);
            }
        })
      });
  </script>
</html>